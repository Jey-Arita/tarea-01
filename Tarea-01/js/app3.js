let primer = prompt('Ingrese primer numero');
let segundo = prompt('Ingrese segundo numero');

let multiplicacion = parseInt(primer) * parseInt(segundo);
let division = parseInt(primer) / parseInt(segundo);

alert(`La Multiplicacion de ${primer} * ${segundo} es: ${multiplicacion}
    La division de  ${primer} / ${segundo} es: ${division}`);